# Neon Postgres (Vercel Marketplace → Storage → Neon)
# Never commit real credentials. Set these in Vercel Project Settings → Environment Variables.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Public site URL (no trailing slash)
NEXT_PUBLIC_SITE_URL="https://yildizhesap.net"

# Payment / integrations (fill later — never commit secrets)
# SHOPIER_API_USER=
# SHOPIER_API_KEY=
# CRYPTOMUS_MERCHANT_ID=
# CRYPTOMUS_API_KEY=
# TELEGRAM_BOT_TOKEN=
